PORT=3456
# Kosongkan = pakai JSON lokal (data/db.json). Isi untuk pakai Mongo (Vercel/serverless):
MONGODB_URI=
MONGODB_DB=zero-portfolio
# Login admin (default ZeroTzy / ZeeAjah — GANTI di production!)
ADMIN_USER=ZeroTzy
ADMIN_PASS=ZeeAjah
# Secret untuk seal challenge AES (GANTI dengan string random panjang di production!)
ADMIN_SECRET=ganti-dengan-random-minimal-32-karakter
SESSION_TTL_HOURS=12
# Legacy token (opsional, untuk kompatibilitas API lama). Kosongkan jika tidak dipakai.
ADMIN_TOKEN=
# Base CDN upload
CDNZERO_BASE=https://api-cdnzero.vercel.app
